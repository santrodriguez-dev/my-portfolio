# My Portfolio

This is a personal portfolio that includes a CV created with HTML and CSS, using Astro collections and Tailwind.

## Project Description

This project is a personal portfolio website that includes:

- A main page with basic information and links.
- A detailed CV section with work experience, education, skills, and more.
- Uses Astro Content Collections to manage structured data like educations, jobs, skills, etc.
- Styled with Tailwind CSS for a modern and responsive design.
- Reusable components for sections like biography, timeline, etc.

## Screenshots

![Portfolio screenshot](/public/sreenshot-resume.png) <!-- Replace with the actual path -->

## 🚀 Project Structure

```text
/
├── public/
├── src/
│   └── pages/
│       └── index.astro
└── package.json
```

## 🧞 Commands

All commands are run from the root of the project, from a terminal:

| Command                | Action                                           |
| :--------------------- | :----------------------------------------------- |
| `pnpm install`         | Installs dependencies                            |
| `pnpm dev`             | Starts local dev server at `localhost:4321`      |
| `pnpm build`           | Build your production site to `./dist/`          |
| `pnpm preview`         | Preview your build locally, before deploying     |
| `pnpm astro ...`       | Run CLI commands like `astro add`, `astro check` |
| `pnpm astro -- --help` | Get help using the Astro CLI                     |
